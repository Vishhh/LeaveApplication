<%= form_for :holidays do |f| %>
<div class="row">
  <div class="col-lg-10">
    <div class="row">
      <div class="col-lg-10" id="example_filter">
        <div class="row search-row">
          <div class="col-md-6 pull-left">
            <% if current_user  %>
            <%= link_to 'New Hardware', new_hardware_path, { :class => 'btn btn-success' } %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<%#= render_filterrific_spinner %>
<% end %>